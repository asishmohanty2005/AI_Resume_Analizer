{
  "name": "resume-analyzer-verification",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "test:browser": "playwright test",
    "lint": "eslint js tests *.cjs"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "@playwright/test": "^1.58.0",
    "eslint": "^9.0.0"
  }
}
